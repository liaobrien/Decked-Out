<section class="card"> 
    {{#each .}}
  <!-- This is an ahref that will display the deck name as a link, it will then lead to deck/:id TBD on those pages -->
   <!-- The card contains the deck name as the card header -->
<ul>
   <li> {{question}}</li>
</ul>
   {{/each}}
</section>